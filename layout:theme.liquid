<!doctype html>
<html class="no-js" lang="{{ shop.locale }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>{% if template == 'index' %}{{ shop.name }}{% else %}{{ page_title }}{% endif %}</title>

  {{ content_for_header }}

  {% comment %}
    Load your theme stylesheets here
  {% endcomment %}
  <link rel="stylesheet" href="{{ 'styles.css' | asset_url }}">

</head>
<body>

  {{ content_for_layout }}

  {% comment %}
    Load your theme scripts here
  {% endcomment %}
  <script src="{{ 'main.js' | asset_url }}"></script>

</body>
</html>
